[project]
name = "brave-core-bot"
version = "0.1.0"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts", "."]

[tool.ruff]
target-version = "py39"
exclude = ["brave-core-tools"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
